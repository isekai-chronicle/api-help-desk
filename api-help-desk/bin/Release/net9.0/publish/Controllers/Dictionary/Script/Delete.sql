-- DECLARE @id UNIQUEIDENTIFIER = @id_parameter,
--         @task_id UNIQUEIDENTIFIER = @task_id_parameter;

-- UPDATE hi_finance.journal.admonitionType SET task_id = @task_id
-- WHERE id = @id;

-- DELETE FROM hi_finance.journal.admonitionType
-- WHERE id = @id;